# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/pipe.rb'

Hoe.new('pipe', Pipe::VERSION) do |p|
  p.developer('Drew Olson', 'olsonas@gmail.com')
  p.remote_rdoc_dir = ''
  p.extra_deps << ['hpricot','>= 0.6.164']
end

# vim: syntax=Ruby
